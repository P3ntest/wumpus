import { Message, MessageEmbed, TextChannel } from "discord.js";

export function help(message: Message, command: string, args: string, config: any) {
  message.channel.send("hahah stfu, imagine being a nerd lmao");
}